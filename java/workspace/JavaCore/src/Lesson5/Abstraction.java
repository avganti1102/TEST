package Lesson5;

public class Abstraction {
	/*
	 * --------Trừu tượng----
	 * -Abstract class mang đầy đủ tính chất của class bình thường
	 * -Có ít nhất 1 phương thức abstract
	 * -Không thể khởi tạo instance
	 */
	
	
	/*
	 * -----Object
	 * - Là một lớp bình thường
	 * 
	 * 
	 * instance--------
	 * -Khi tạo ra 1 thực thể của Object đấy là tạo ra 1 instance
	 */
	
	/*
	 * ----------Interface-----------
	 * - Là một bộ các phương thức lquan đến hành vi
	 * - Là một lớp không có thuộc tính
	 * - Chỉ bao gồm các phương thức public abstract (Default: public abstract)
	 * - Không thể tạo ra instance
	 * - 1 lớp có thể implements được nhiều Interface
	 */
}

package Lesson5;

public class Polymorphism {
	/*
	 * - Class reference(Data type) refer Objects
	 * VD: Person p = new Student();
	 * - Áp dụng cho các bài toán lquan đến quản lý ( array(cán bộ))
	 */
	
	/*
	 * --3 kiểu quan hệ trong Java
	 * - Has - a: Bên trong 1 Obj có 1 kiểu dữ liệu là 1 Obj khác
	 * - Is - a: Person p = new Student();
	 * 	  		 Person p = new Teacher();
	 * - Song song: Không thuộc hai kiểu quan hệ kia
	 * -SuTu st1 = new DongVat(); --->Sai--Chỉ có thể trỏ đến: + Chính nó
															// + Con của nó
	   - Chỉ gọi được các phương thức thuộc kiểu tham chiếu. Nhưng khi sử dụng sẽ sử dụng phương thức của kiểu MỞ RỘNG HƠN

	 */
		/*
	 * ------instanceof----
	 * p instanceof Student >> true
	 * <biến> + instanceof + <Kiểu dữ liệu> >>>> true/false
	 */
}

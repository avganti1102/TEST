package Lesson6;

public class throwAble {
	/*
	 * Compile time: quá trình từ .java -> .class
	 * Runtime: Khi play Java Application(alt+shift + x)
	 */
	/*
	 * --------Throwable gồm: -------
	 * - Error
	 * - Exception: + IO Exception (Input/Output)
	 * 				+ Runtime Exception
	 * 
	 * --Phân biệt:
	 * + Error thường là lỗi tại nơi mà ta k thể can thiệp đc ( out of memory
	 */
}

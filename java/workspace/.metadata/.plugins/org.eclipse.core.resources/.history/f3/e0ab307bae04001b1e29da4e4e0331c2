package Lesson11;

public class InnerClass {
	/*----InnerClass------
	 *		- Là khai báo một lớp trong một lớp khác
	 */	
	
	/*
	 * GarbageCollector is:
	 * - Đối tượng được tạo ra mà k có con trỏ nào trỏ đến
	 * - Parent của đối tượng k có con trỏ nào trỏ đến
	 * - Local var
	 */
}

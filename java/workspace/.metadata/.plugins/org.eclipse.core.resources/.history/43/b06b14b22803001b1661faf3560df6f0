package Lesson10;

import java.util.Comparator;

public class SortByPoint implements Comparator<Person>{

	@Override
	public int compare(SortByPoint o1, SortByPoint o2) {
		if(o1.getM > o2)
			return 1;
		else if(o1 < o2)
			return -1;
		else
		return 0;
	}

}
